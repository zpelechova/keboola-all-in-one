It takes 05_pricehistory and 04_extension datasets and transforms them in order to be compatible with AWS S3 processor which transforms each table row into separate json file, where shop_id and slug are primary keys.