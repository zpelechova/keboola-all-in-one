Creates **table for pairing url with itemId on the web** - it parses the url so that it only has the piece which is both unique for the product and universal for any url access (if there is www at the beginning or not, if there is affilliation link etc.) - **metadata table**